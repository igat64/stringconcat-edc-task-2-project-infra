# EDC Task 2 - Project Infrastructure

### Prerequisites

Make sure the following dependencies installed:

- Docker
- Node.js

### Start the Project

Run the app:

```shell
./run.sh
```

or run the tests:

```shell
./test.sh
```
